name: keepalive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
permissions: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping healthz
        run: curl -s -m 30 https://rep-finance.onrender.com/healthz